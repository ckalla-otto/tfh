# Smoke-test config: tiny equal-per-class budget, short run.
# Usage: python -m pad prepare --config configs/exp_smoke.yaml ...  (then train)
include: [base.yaml]

data:
  subsets_dir: data/subsets_smoke
  subset:
    budget_total: 5000

train:
  epochs: 3
  batch_size: 24
  log_every: 5