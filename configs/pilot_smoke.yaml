# Tiny pilot for the subset-only download path validation.
include: [base.yaml]

out_dir: results/dev_pilot

data:
  crawl_meta: data/crawl.csv
  subsets_dir: data/subsets_pilot
  subset:
    budget_total: 300     # ~10 per class per split
