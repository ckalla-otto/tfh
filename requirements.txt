# Core (works on CPU/MPS for local dev and CUDA on the T4 VM)
torch>=2.1
torchvision>=0.16
timm>=0.9
numpy
pandas
scikit-learn
PyYAML
tqdm
matplotlib
opencv-python-headless

# Pseudo-depth generation (Depth-Anything V2 via HF Transformers)
transformers>=4.40

# Kaggle download of the CelebA-Spoof mirror
kaggle

# Test
pytest