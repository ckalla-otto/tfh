# Optional env overrides (loaded automatically via python-dotenv by `import pad`).
# No Kaggle credentials are needed anymore — `pad prepare` reads the on-disk
# mirror + `data/labels/label.csv` directly.
# PAD_ENV=/absolute/path/to/.env   # if you keep the .env elsewhere
# PAD_DATA_ROOT=/path/to/CelebA_Spoof